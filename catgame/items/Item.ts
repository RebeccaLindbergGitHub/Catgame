export class Item {
  yPos: number;
  xPos: number;
  symbol: string;

  constructor(yPos: number, xPos: number, symbol: string) {
    this.yPos = yPos;
    this.xPos = xPos;
    this.symbol = symbol;
  }
}
